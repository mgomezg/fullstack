version: '3'

services:
  backend:
    build: .
    command: python3 manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/code
    ports:
      - "8000:8000"
    networks:
      - webappnetwork
  frontend:
    build: ./front
    volumes:
      - ./front:/front
      - /app/node_modules
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
    depends_on:
      - backend
    command: npm start
    networks:
      - webappnetwork
networks:
    webappnetwork:
        driver: bridge
